import { createAction } from '@ngrx/store';

export const increment = createAction('[Counter Component] 1');
export const decrement = createAction('[Counter Component] 2');
export const reset = createAction('[Counter Component] 3');
